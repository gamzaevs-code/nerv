'use client';
import { useRouter } from 'next/navigation';
import { FormEvent } from 'react';
export function AdminChallengeForm() { const router = useRouter(); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); await fetch('/api/challenges', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(Object.fromEntries(new FormData(e.currentTarget).entries())) }); e.currentTarget.reset(); router.refresh(); } return <form className="form" onSubmit={submit}><input name="title" placeholder="Название" required /><textarea name="description" placeholder="Описание" /><input name="reward" type="number" placeholder="Награда" required /><select name="difficulty"><option value="easy">easy</option><option value="medium">medium</option><option value="hard">hard</option></select><select name="category"><option value="daily">daily</option><option value="weekly">weekly</option></select><input name="imageUrl" placeholder="Image URL" /><button className="neon-button">Создать челлендж</button></form>; }
export function DeleteChallengeButton({ id }: { id: number }) { const router = useRouter(); return <button className="neon-button-outline" onClick={async () => { await fetch(`/api/admin/challenges/${id}`, { method: 'DELETE' }); router.refresh(); }}>Удалить</button>; }
